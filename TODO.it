1) make html body a css grid.
2) make a big box where the other css grid (link-container) fits in
3) give all the area attributes
4) Give the settings form a area that overlays the rest
5) blur the rest.